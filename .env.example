# ADMIN_PASSWORD: Password for /admin CMS login (required in production).
# Set this to a private, non-empty value before deploying.
ADMIN_PASSWORD=

# PORT: HTTP port for the Express server (default 3000).
PORT=3000
